const goWorkout = (e) => {
    e.preventDefault();
    navigate('/workout');
  };
  const goHome = (e) => {
    e.preventDefault();
    navigate('/home');
  };
 
  const goActivities = (e) => {
    e.preventDefault();
    navigate('/activities');
  };

  const goCalendar = (e) => {
    e.preventDefault();
    navigate('/calendar');
  };
